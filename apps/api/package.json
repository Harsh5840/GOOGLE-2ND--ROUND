{
  "name": "@city-project/api",
  "version": "1.0.0",
  "description": "City Project API Service",
  "main": "src/main.py",
  "scripts": {
    "dev": "python src/main.py",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step required for Python'",
    "test": "python -m pytest",
    "lint": "ruff check .",
    "type-check": "mypy src/"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}